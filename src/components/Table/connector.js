const connector = ({
    table: {
        lootfarm,
        rustTm,
        valute,
        settings,
    },
}) => ({
    lootfarm,
    rustTm,
    valute,
    settings,
});

export default connector;
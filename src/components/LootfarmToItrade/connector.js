const connector = ({
    main: {
        lootfarm,
        itrade,
    },
    lootfarmToItrade: {
        settings,
    },
}) => ({
    lootfarm,
    itrade,
    settings,
});

export default connector;
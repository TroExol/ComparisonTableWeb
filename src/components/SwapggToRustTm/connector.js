const connector = ({
    main: {
        swapgg,
        rustTm,
        valute,
    },
    swapggToRustTm: {
        settings,
    },
}) => ({
    swapgg,
    rustTm,
    valute,
    settings,
});

export default connector;